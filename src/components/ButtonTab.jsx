import React from 'react'

export const ButtonTab = ({id, toggleTab, buttonContent}) => {
  return (
    <button>
        <img src="" alt="" />
        <span>{buttonContent}</span>
    </button>
  )
}
